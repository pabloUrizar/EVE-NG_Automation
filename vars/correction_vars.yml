# Directory containing the Python scripts
script_dir: "../lab_correction_scripts"

# Source directory for the .unl files of the students
source_dir: "../unl_files"

# Output directory for parsed files
parsed_dir: "../parsed_files"

# Directory for reports generated by the second script
reports_dir: "../reports"

# Solution JSON file to compare to
solution_file: "../parsed_files/7-DNS_OK.json"
